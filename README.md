# Perla — release feed

This repo hosts Perla's **Sparkle update feed** (`appcast.xml`) and the
**notarized app binaries** (attached to GitHub Releases). It contains **no source code**.

- Feed: https://inawafalm.github.io/perla-releases/appcast.xml
- Binaries: see the Releases tab.
